# FastAPI backend for AI Growth Analyst
fastapi>=0.110
uvicorn[standard]>=0.29
gunicorn>=21.2
pydantic>=2.6
pydantic-settings>=2.2

# Database
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1        # PostgreSQL driver (Neon/Supabase/Railway)

# Auth & security
authlib>=1.3               # Google OAuth
httpx>=0.27                # required by authlib's starlette client
itsdangerous>=2.1          # signed session cookies
python-jose[cryptography]>=3.3
cryptography>=42.0         # Fernet encryption for creds at rest

# Reused by analysis/connectors (already used by the Streamlit app)
pandas>=2.0
requests>=2.31
google-generativeai>=0.8
google-analytics-data>=0.18
google-api-python-client>=2.130
google-auth>=2.30
google-auth-oauthlib>=1.2
